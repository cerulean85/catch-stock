# 2026-05-07 — screener (initial implementation)

## 한 일

- `docs/spec/CONCEPT.md` 보강 + `docs/spec/screener.md` 신규 (필터·API 계약·파이프라인·비범위 정리)
- Next.js 16 + React 19 + Tailwind 4 + shadcn 스캐폴드
- Vitest + Testing Library 구성 (`vitest.config.ts`, `src/test/setup.ts`)
- FSD 폴더 정리: `src/features/screener/{ui,model,api}` + `src/shared/{lib,constants}`
- `src/shared/constants/sp500.json` (대표 종목 100개; 추후 전체 500개로 교체 가능)
- `src/shared/lib/rsi.ts` Wilder RSI14 + 7개 단위 테스트 (상수/단조/혼합 검증)
- `src/shared/lib/yahoo.ts` yahoo-finance2 어댑터 (`chart()` 호출, 일봉 ~120일 / 월봉 ~36개월)
- `src/features/screener/model/screener.ts` 순수 필터 로직 + 4개 단위 테스트
- `src/features/screener/api/server.ts` p-limit 동시성 10, 메모리 캐시 TTL 15분
- `src/app/api/screener/route.ts` Route Handler (`refresh=1` 지원, 5xx JSON 에러)
- `src/features/screener/model/store.ts` Zustand 클라이언트 스토어
- `src/features/screener/ui/{ScreenerView, ScreenerGrid}.tsx` shadcn Table 기반 UI

## 검증

- `npm run test`: 11/11 통과
- `npm run build`: 성공 (`/api/screener` 동적, `/` 정적)
- 런타임 smoke (`npm run dev`):
  - cold `GET /api/screener`: 200, 9.9s, 6개 종목 반환
  - hot 캐시 hit: 9ms, `cache.hit=true`
  - `GET /`: 200

## 결정 사항

- 데이터 소스: `yahoo-finance2` v3 (`new YahooFinance()`, `chart()` 사용). 키 불필요.
- 캐시: 모듈 전역 Map (Node 런타임). Vercel 배포 시 인스턴스별 캐시 → 충분.
- S&P 500 목록: 정적 JSON. 운영 시 전체 500개로 교체.
- 컴퓨팅: 100% 서버 사이드 (`runtime = 'nodejs'`).

## 남은 일 / 후속

- 전체 S&P 500 종목 리스트로 교체 (현재 100개 표본).
- 필터 UI 노출(min/max 슬라이더 등). 현재는 기본값 고정.
- 컬럼 정렬 토글(현재 monthlyRSI14 desc 고정).
- 캐시 외화: Vercel KV / Upstash 등으로 이주 (멀티 인스턴스 정합성).
- 통합 테스트: 모킹된 yahoo로 `getScreenerResult` 골든 케이스 추가.
